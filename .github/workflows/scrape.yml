# name: Run MDL Scraper Batch

# on:
#   schedule:
#     - cron: '0 */4 * * *'  # Every 4 hours
#   workflow_dispatch:       # Manual trigger support

# permissions:
#   contents: write  # ✅ Allows pushing to the repo

# jobs:
#   scrape:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout repo
#       uses: actions/checkout@v3
#       with:
#         persist-credentials: true  # needed for pushing

#     - name: Set up Python
#       uses: actions/setup-python@v5
#       with:
#         python-version: '3.10'

#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install requests

#     - name: Run scraper
#       run: python scraper.py

#     - name: Commit and push results
#       run: |
#         git config --global user.name 'github-actions'
#         git config --global user.email 'github-actions@github.com'
#         git add output_batch_*.json failed_batch_*.json progress.json
#         git commit -m "🧹 Batch run: update output and progress [skip ci]" || echo "No changes to commit"
#         git push
